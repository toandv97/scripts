#!/bin/bash

source common

# Check if the source branch parameter is provided
if [ -z "$1" ]; then
    MSG="mirror update"
else
    MSG=$1
fi

git status

git add .

git commit -m "$MSG"

git push "${@:2}"
check_cmd "Failed to push code to remote"

exit 0
